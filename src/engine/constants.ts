export const CORE_TEXT_FILES = ["history.jsonl", "config.toml", "version.json"] as const;
export const CORE_DIRS = ["sessions", "rules", "skills"] as const;
export const AUTH_FILES = ["auth.json", "cap_sid"] as const;

export const BACKUP_FORMAT = "codex-backup-v3";
